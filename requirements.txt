# AI Candidate Screening Pipeline MVP
# Production dependencies for automated resume screening system

# Core API client for Claude integration
anthropic>=0.28.0

# Optional: for enhanced resume parsing if needed in future versions
# pydantic>=2.0.0

# Optional: for PDF resume support (future enhancement)
# pypdf>=4.0.0

# Optional: for database integration (future enhancement)
# sqlalchemy>=2.0.0

# Development dependencies (install with: pip install -r requirements.txt -e ".[dev]")
# pytest>=7.4.0
# pytest-cov>=4.1.0
# black>=23.0.0
# flake8>=6.0.0
# mypy>=1.5.0
